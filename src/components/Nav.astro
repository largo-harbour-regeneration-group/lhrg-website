<nav class="nav" aria-label="Primary">
	<details class="nav-details">
		<summary class="nav-toggle">
			<span class="visually-hidden">Menu</span>
			<span class="nav-toggle__bar"></span>
			<span class="nav-toggle__bar"></span>
			<span class="nav-toggle__bar"></span>
		</summary>
		<ul class="nav__menu">
			<li><a href="#about">About</a></li>
			<li><a href="#people">People</a></li>
			<li><a href="#gallery">Gallery</a></li>
			<li><a href="#contact">Contact</a></li>
		</ul>
	</details>
</nav>

<style>
	.nav {
		display: flex;
		align-items: center;
	}

	.nav-toggle {
		display: flex;
		align-items: center;
		gap: 4px;
		flex-direction: column;
		list-style: none;
		cursor: pointer;
		padding: var(--space-2);
	}

	/* Hide the native disclosure marker (browsers render it differently). */
	.nav-toggle::-webkit-details-marker {
		display: none;
	}

	.nav-toggle::marker {
		content: '';
	}

	.nav-toggle__bar {
		width: 22px;
		height: 2px;
		background: var(--color-white);
	}

	.nav__menu {
		list-style: none;
		margin: 0;
		padding: var(--space-3);
		position: absolute;
		top: 100%;
		left: 0;
		right: 0;
		display: flex;
		flex-direction: column;
		gap: var(--space-2);
		background: var(--color-navy-dark);
	}

	.nav__menu a {
		position: relative;
		display: inline-block;
		color: var(--color-white);
		text-decoration: none;
		font-weight: 600;
		padding-bottom: 2px;
	}

	.nav__menu a::after {
		content: '';
		position: absolute;
		left: 0;
		right: 100%;
		bottom: 0;
		height: 2px;
		background: var(--color-teal-bright);
		transition: right 0.2s ease;
	}

	.nav__menu a:hover::after,
	.nav__menu a:focus-visible::after {
		right: 0;
	}

	/* Above the mobile breakpoint, show the links as a plain row and hide the
	   disclosure toggle entirely — the !important overrides the browser's own
	   `details:not([open]) > *:not(summary) { display: none }` rule so the
	   menu renders "open" without any JS. This is deliberate, not a bug. */
	@media (min-width: 641px) {
		.nav-details {
			display: contents;
		}

		.nav-toggle {
			display: none;
		}

		.nav__menu {
			display: flex !important;
			flex-direction: row;
			position: static;
			background: none;
			padding: 0;
			gap: var(--space-4);
		}
	}
</style>
